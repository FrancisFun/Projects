
//服务器ip和端口
 let hostAndPort = 'http://<HOST AND PORT>'
//let hostAndPort = 'http://127.0.0.1:6060'

let fileServerHostAndPort = 'http://<HOST AND PORT>'

module.exports = {
  hostAndPort, fileServerHostAndPort
}
